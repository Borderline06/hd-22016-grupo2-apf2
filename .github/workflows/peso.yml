name: Check File Sizes

on: [push, pull_request, workflow_dispatch]

jobs:
  size-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check file sizes
        run: |
          du -h assets/JavaScript/*
          du -h assets/Styles/*
